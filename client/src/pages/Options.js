import React, { useState, useEffect } from 'react';

export default function Options() {

    return (
        <div>
            Options page where couple/group can add options from master list to their list of options to vote on/rank
        </div>
    );
}